<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HomePage</title>
    <!-- Include Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body>
    <nav>
        <div class="flex items-center justify-between bg-blue-500 p-4">
            <h1 class="text-white text-2xl font-semibold">Home</h1>
            <% if (locals.user) { %>
                <a href="/logout" class="text-white">Logout</a>
            <% } else { %>
            <a href="/signin" class="text-white">Sign in</a>
            <% } %>

        </div>
    </nav>

    <!-- searchbox -->
    <div class="flex items-center justify-center h-screen">
        <div class="max-w-md w-full p-6 bg-white rounded-lg shadow-md">
            <h2 class="text-xl font-semibold mb-10 text-center">Search</h2>
            <form action="/search" method="post" id="searchForm" class="space-y-6">
                <div>
                    <input type="text" id="search" name="search" class="w-full border-gray-300 rounded-md" placeholder="Search...">
                </div>
                <button type="submit"
                    class="w-full bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600 transition-colors duration-300">Search</button>
            </form>
        </div>
    </div>

</body>

</html>